.container
    .row
        .col-md-12
            h1 Server status
            button.btn.btn-success Get Currency
            div(ng-repeat="curr in currency.xml") {{curr.currency}} {{curr.rate}}
            table
                tr
                    th one
                    th two
                tr(ng-repeat="cur in currency.xml")
                    td {{cur.currency}}
                    td {{cur.rate}}
